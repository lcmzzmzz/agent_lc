"""
server 包初始化模块

【正经注释】
本模块是 backend.server 包的初始化文件，负责将 server 目录标识为 Python 包。
该包包含 GPT Researcher 后端服务的核心组件，包括 FastAPI 应用实例、WebSocket 管理器、
Agent 发现协议、日志配置、多代理运行器、报告存储以及服务端工具函数等模块。

【大白话注释】
这个文件就是个占位符，让 Python 知道 server 文件夹是一个包（package）。
里面啥代码都没有，真正的干活代码都在同目录下的其他文件里。
"""
